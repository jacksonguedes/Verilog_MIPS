module somador4b(input[3:0] a, b, 
                 input selecao,
					  output reg [3:0] saida);
						
		 always @(*)
			if (s == 0)
				saida = a + b;
			else
				saida = a - b;
			
endmodule
					  
					  